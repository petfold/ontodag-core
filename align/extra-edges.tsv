# sub	sup	note — edges the pack asserts about names it does not itself define (the prelude's kind nodes).
# Peter, 2026-09-02: measured dimensions are attributes. Kind level, so every present and future head
# inherits; additive to the registry (kind resolution stops at the kind node). geo-dimension is left out:
# a geo value is a place. The pack therefore presumes the prelude (pack zero).
linear-dimension	attribute	weight, length, area, volume, speed, pressure, temperature, energy, size, duration
count-dimension	attribute	count
calendar-dimension	attribute	time
